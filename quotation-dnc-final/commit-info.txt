COMMIT INFORMATION
==================

BRANCH
  claude/quotation-dnc-audit-repair-ashi82

STARTING COMMIT (pass 1 baseline)
  b5493089057277c6f7742931da26bc6f35553abd

DELTA BASELINE (pass 2 baseline, as given in the brief)
  744ad4084167bf3e0638535779b798b5023c0030

MINI-DELTA BASELINE (pass 3)
  7d0981fdb0f83a0b76a4c3d6b8a3ad1a80e3f38a

LIVE-ACCEPTANCE BASELINE (pass 4)
  f250426346c1a256350fca2b12c54f1f5de034b4

QUICK ADD HISTORY BASELINE (pass 5)
  8c43da75e2110772ad9a4d7d744c9491c0181c5e

QUICK ADD LAYOUT BASELINE (pass 6)
  5c2c78cbc3cdf61a776f8786c50d5300f7077262

EXTRACTION BASELINE (pass 7)
  6bba600191a306af2e70c3d37f37f1b052099cf4

EXTRACTION COMMIT (pass 7, part 1)
  f074b5b111fc93698968ae704fc1b3443cab6ac2

QUICK ADD REPAIR (pass 7, part 2)
  581d5024e0e68426de27016d742ed28139919934

MATERIAL IDENTITY (pass 7, part 3)
  ddc2c87666244aad786ee4a3add14f8e1897847c

PREVIOUS PRICE (pass 7, part 4)
  2345502ed5d324ca97ed3a6fe71353ff097b1085

HISTORY IDENTITY (pass 7, part 5)
  0f4f184b8487e73803e0506b475ba2465c711a9d

LIVE FAILURE REPAIR (pass 7, part 6)
  16b5f3c  Imperial pricing history, and an unanswered size type

DEPLOY COMMIT  <-- deploy this one
  7932c2c  A review screen, a thread that is not a size, and one price on many rows

  This is the last commit that changes an application file. The packaging
  commit that follows it adds only this file and the ZIP, both of which are
  outside .cpanel.yml's allowlist and are never copied to the server.

  It carries 0f4f184 with it, and both are needed for the reported live case:
  0f4f184 is what makes a differently-coated record appear as a labelled
  reference, and 16b5f3c is what makes an imperial record reachable at all.

DEPLOYMENT STATUS
  NOT DEPLOYED

  .cpanel.yml is a manual two-click deploy and nothing here runs on its own:

    cPanel -> Git Version Control -> Manage
      1. "Update from Remote"    pulls the branch into the server-side clone
      2. "Deploy HEAD Commit"    copies the allowlisted files

  I cannot press those buttons from this environment, and no part of this
  report describes the live site as verified.

FILES THE DEPLOY WILL COPY (changed by this work)
  index.php
  api.php
  companies.php
  ai_extract.php
  pricing_history.php          (new; added to APPFILES in .cpanel.yml)

FILES THE DEPLOY WILL NOT COPY (outside the allowlist, by construction)
  tests/                       the regression suite and its tools
  quotation-dnc-final/         this package
  everything else not named in APPFILES

DATABASE
  No schema change. No migration. No data written by this work.

SECRETS
  None in the repository, none in this package. auth.php, db.php and
  ai_config.php are untouched and remain server-only.

COMMITS ON THIS BRANCH
  2e2bbd9  Nothing on the screen may disagree with the number under it
  0c264e8  Audit package: commit info and changed-files summary
  744ad40  Five findings that survived being argued with
  9d70d60  Audit package: stamp the commit to deploy
  323e92b  A company rule is an answer; a diameter has one source
  576fde5  Pricing history: the rows we sent, and why they differed
  9561291  A correction is not a suggestion, and a short list is not a whole one
  21a4bf7  Pricing Engine V2: prepared, documented, and switched off
  511a697  Restore CRLF line endings in index.php and api.php
  0cba90b  Audit package: stamp the commit to deploy
  fba453a  The same defect, through a second door
  7d0981f  Audit package: stamp the commit to deploy
  f250426  A bolt's price is the bolt's
  7c87951  Audit package: stamp the commit to deploy
  8c43da7  The drawing's own words, and whose dimension is whose
  17550f1  Audit package: stamp the commit to deploy
  5c2c78c  This row's history, on this row
  4b042b7  Audit package: stamp the commit to deploy
  6bba600  A span counted from the end of the track list
  3a27cc3  Audit package: stamp the commit to deploy
  f074b5b  A hook is a hook, and a grade is not a steel
  4e2ed7e  Audit package: the hook, the sideways table, and the evidence
  ef3d191  The scratch the evidence is made in is not the evidence
  581d502  The screen said the row was ready and the button disagreed
  b1ef3d5  Audit package: the click that did nothing
  ddc2c87  Four materials, one vocabulary, and the same answer whatever the source
  89c5f13  Audit package: four materials, and the evidence for both live formats
  2345502  A previous price is a recipe, and an old one must still be findable
  b4eb724  Audit package: the recipe, and the record that could not be found
  0f4f184  The record has to still be this item's when the button is pressed
  f84ada8  Audit package: whose price is this
  16b5f3c  Imperial pricing history, and an unanswered size type
  9483a3d  Audit package: the size the query could not ask for
  7932c2c  A review screen, a thread that is not a size, and one price on many rows

REGRESSION AT 7932c2c
  28 browser suites         2480 assertions   0 failed
  ai_extract (PHP)           107 assertions   0 failed
  pricing history (PHP)      161 assertions   0 failed
  pricing workbook (Python)   62 assertions   0 failed
  ------------------------------------------------------
  TOTAL                     2810 assertions   0 failed
  PHP lint                    10 files, no syntax errors

AFTER DEPLOYMENT — the checks I cannot perform
  Run HAB-TA-01.pdf and the 29-row anchor-bolt screenshot through the live
  Analyze path once, and compare against 950 / 865 / 1000 / 1200 / 1285 and
  29 rows. Everything downstream of the model's answer is proven by the suite:
  the extraction production actually returned — rows 3 and 5 ambiguous — is a
  fixture in tests/suites/15-dimension-schema.test.js and resolves to 1000 and
  1285 without manual confirmation. What still needs a live API key is whether
  the improved prompt makes the model place those dimensions correctly in the
  first place, so that elimination is not needed at all.

  Pass 7 adds two of the same kind. Run the J-shaped bolt drawing and the
  rotated stud table through the live Analyze path once and confirm the model
  now returns product J_BOLT for the hook, and reads the rotated table's two
  merged specification cells onto the rows they cover. Everything downstream
  of those answers is proven by tests/suites/19-drawing-interpretation.test.js
  against fixtures shaped exactly like the reported failures; the schema and
  the instruction text are proven by tests/php/ai_extract.test.php. What needs
  a key is the model's own reading of the page.

  Pass 7 part 2 adds the live acceptance checks for the Quick Add repair. Add
  the corrected J Bolt and a Sag Rod from Quick Add and confirm each reaches
  the Quotation List, with no generic "0 of 1 added" on a valid row. Confirm a
  stainless item carries no finish on the quotation, on the print sheet and in
  the WhatsApp text.

  Pass 7 part 3 adds the material ruling. Run the photographed HT8.8 order list
  and HAB-TA-01 through the live Analyze path and confirm 4140 QT / ZP and
  4140 QT / PL respectively, with no Needs Material, and Case B's rows 3-6 as
  4140 QT / HDG carrying GRADE 8.8 as evidence.

  Pass 7 part 4 adds the Previous Price repair. Confirm against real data that
  a 4140 QT quotation older than the newest few hundred now appears in
  Previous Prices, and that reusing Q-2026-0366 puts 6.50 / 3.50 / 4% / Auto
  Round on the row rather than a Manual Price of RM 6.84.

  Pass 7 part 5 adds the history identity acceptance check. Confirm that a
  1/2" MS UNDERSIZE ZP rod shows its earlier PL quotations as references,
  labelled and not reusable, nearest length first — and that changing a row's
  material takes any open history card away with it.

  Pass 7 part 6 adds the two live failures. Against real data, confirm that
  1/2" MS UNDERSIZE ZP L1020 now returns Q-2026-0470's two PL records rather
  than "No pricing history for this exact specification", and that the other
  imperial sizes — 3/8, 5/8, 3/4, 7/8, 1-1/4, 1-1/2 — return their own history
  too. This is the one check that cannot be made without the database: the
  fault was in the SQL the endpoint sends, and the suite proves the query is
  now correct through MySQL's own LIKE semantics, not that your rows match it.

  And confirm on screen that a row whose Size Type nobody has stated shows a
  dash for the weight and a dash for the price, and that choosing Undersize or
  Fullsize computes immediately.
