CHANGED FILES — both audit passes
=================================
Branch: claude/quotation-dnc-audit-repair-ashi82
Pass 1 baseline: b5493089057277c6f7742931da26bc6f35553abd
Pass 2 baseline: 744ad4084167bf3e0638535779b798b5023c0030

APPLICATION FILES (deployed by .cpanel.yml)
-------------------------------------------
index.php
  Pass 1  · imperial positional parsing: the first token of a run is the size,
            and a thread standard (UNC/BSW/BSP) never steals the diameter
          · the size box shows the value the row holds, live, mid-typing
          · labelled dimensions (H/W/S/TL/ID) read in the right direction
          · thread evidence stops at the end of its own section
          · a default price no longer outlives the item it was fetched for
          · undersize inch sizes stopped falling through to the fullsize table
          · four value leaks between rows of one Quick Add session closed
          · five stale warnings cleared when their cause is corrected
          · accessories are charged where they are promised
          · copy-quotation no longer carries the original's date

  Pass 2  · DC_SIZE_TYPE_RULES: one central source for the company's size-type
            defaults, with a badge naming where each applied default came from
          · dcEffectiveDiameter / dcBuiltInDiameter: one resolver for the
            diameter; the rate table's competing `dia` field deleted; the
            Diameter Settings screen rendered from the same resolver
          · Pricing History replaces Last Price: fetch, ranking, pagination,
            per-record cost rate / additional cost / markup / bolt unit price,
            accessory cost stated separately, no averages anywhere
          · wqaChangeProduct: corrections and deletions survive choosing a
            product; the Selected-Items scope is obeyed. Seven paths that write
            into rows — Correct Items, the Common Fields header, the Pricing
            panel, Apply Manual Price, Apply/Clear Accessories and the per-row
            accessory editor — now mark the rows they change, so a correction
            made from a panel survives too
          · buildWAItemsText: message lines carry the quotation's own item
            numbers; merged twins carry both
          · partial extraction: a banner with the recovered count, and Add Items
            gated on an explicit acknowledgement
          · displayItemDesc unchanged, now shared with companies.php

  Pass 3  · dcLineMoney: one place that settles a line's money. A bolt's unit
            price is the bolt's; the accessory charge is its own figure; the
            line total is (bolt + accessories) x qty and is unchanged in money
          · resolvePriceMode no longer adds the accessory charge to the bolt
          · the calculation card, the item card, the print sheet, the WhatsApp
            message and the Quick Add row all show the two figures apart; the
            print sheet gives accessories their own priced row
          · dcUnfoldLegacyManualPrice: an item manually priced before the
            separation has its accessory charge moved out of the typed figure
            when it is edited, leaving the line total exactly as it was
          · phDimValues / phDimDistance / phSortRecords: the closeness score and
            reading order, mirroring pricing_history.php to the millimetre
          · the history record shows unit weight, price mode and how far the
            dimensions differ; a value never recorded says so

  Pass 4  · wqaDimLabel: one vocabulary for every product — L, W, H, ID, S, TL
            — so a Sag Rod's thread is TL exactly as an L Bolt's is
          · wqaRowDimSummary / wqaDimShort: every value carries its label with
            a space, and no value is shown naked
          · wqaResolveAmbiguousLengths: an overall dimension that can only
            belong to one part is assigned by elimination, badged as worked out,
            and left alone whenever the choice is still genuinely open
          · the note behind such a choice is read whole, never from the pill it
            was trimmed into

  Pass 5  · a History action on every Quick Add row, beside Edit and the
            delete, with its own grid track at each breakpoint
          · the history panel moved out of the row's editor, so a collapsed row
            can show it; each row keeps its own open/closed state
          · wqaLoadRowHistory / wqaHistFor / wqaHistStale: a row is looked up
            when it is asked about and never before, reloaded when its identity
            or geometry moves, and re-ranked without a request when only the
            geometry moved. Two rows of one specification share one request
          · no matching, ranking or interpretation rule added: the row calls
            the same phFetch / phDimDistance / phSortRecords / phRecordHtml the
            entry form calls

  Pass 6  · the three row actions became ONE grid cell with a guaranteed width,
            so the layout no longer depends on how many actions there are. The
            badge span counted from the end of the track list (grid-column:
            2/-3) is gone — it was what pushed History and delete off an L Bolt
            and J Bolt row when the History track was added
          · warnings and the row's identity text share ONE thin strip under the
            data line, shown only when there is something to say, so a clean row
            is one line and a warning row is a line plus a strip
          · the modal asks for the width its own columns need, inside
            min(96vw, …), so a J Bolt list gets room and a Stud list does not
            grow
          · the mixed-list specification column is sized for the longest
            product: a J Bolt's TL was being cut off behind an ellipsis
          · the pricing-history panel's styles referenced --surface-2,
            --surface-3 and --brand, none of which this application declares;
            repaired, pricing fields aligned into columns, panel indented under
            its own row

api.php
  Pass 1  · the previous-price lookup respects an unstated finish or size type
            instead of treating it as "any"
  Pass 2  · requires pricing_history.php and uses its shared rules
          · new action get_pricing_history: whole-database search with a SQL
            LIKE prefilter, paginated, ownTotal/otherTotal reported
          · search_items now returns the item's material, so a legacy
            description can be normalised the way the quotation screen does
  Pass 3  · get_pricing_history narrows in the database on both branches — size
            AND material for a modern quotation, size text for a legacy one —
            and both branches only ever narrow
          · get_price_history retired: it read the newest 300 quotations and
            nothing had called it since pass 2

companies.php
  Pass 1  · one display fix carried over from the quotation screen
  Pass 2  · displayItemDesc added; every staff-facing description on the company
            screens goes through it. Display only — nothing stored is rewritten,
            and 4140_PLAIN / Y_BAR never gain a "QT" they do not have
  Pass 3  · dcItemAccUnit / dcAccNote: the three places that print a unit price
            now say what is charged beside it, so quantity x unit price and the
            line total reconcile

ai_extract.php
  Pass 4  · the prompt decides WHICH PART an overall dimension belongs to by
            position — reading order, the part's own band, the extension lines,
            N dimensions for N stacked parts — before it decides anything else
  Pass 1  · dense tables no longer fail as "could not analyze": the response
            ceiling was the cause, not the table
          · a truncated response keeps the rows that arrived whole and reports
            that it was cut off
          · failure causes are distinguishable from one another

pricing_history.php                                              NEW
  Pass 2  · the identity, accessory-separation and ranking rules, with no
            database, session or HTTP dependency, so api.php and the tests share
            one implementation
  Pass 3  · dc_dim_values / dc_dim_distance: the deterministic closeness score
          · dc_history_sort: this customer first, then the nearest rod
          · a record saved with the accessory charge separated is read directly;
            an older one is still never guessed at
          · records carry dimDistance, lineUnitPrice and priceModeLabel

.cpanel.yml
  Pass 2  · one line: pricing_history.php added to the APPFILES allowlist.
            The safety model — allowlist, protected names, two-phase copy,
            post-copy verification — is unchanged

NOT DEPLOYED (outside the .cpanel.yml allowlist by construction)
---------------------------------------------------------------
tests/lib/harness.js            serves the real index.php in Chromium
tests/lib/assert.js             assertion + suite bookkeeping
tests/run.js                    runner
tests/screenshots.js            the nine evidence frames
tests/layout-shots.js           the fourteen layout frames
tests/suites/01 … 18            18 browser suites, 1336 assertions
tests/php/ai_extract.test.php   64 assertions
tests/php/pricing_history.test.php  72 assertions
tests/tools/build-pricing-workbook.py   builds the blank V2 workbook
tests/tools/check-pricing-workbook.py   asserts it holds no business value
quotation-dnc-final/            this package

UNTOUCHED
---------
auth.php · login.php · logout.php · db.sample.php · ai_config.sample.php
php.ini · manifest.webmanifest · assets/

NO DATABASE SCHEMA CHANGE. NO SECRET IN THE REPOSITORY OR IN THIS PACKAGE.

PASS 7 — AI extraction / engineering-document interpretation
-----------------------------------------------------------
Baseline 6bba600191a306af2e70c3d37f37f1b052099cf4

ai_extract.php
  · J_BOLT added to the document-level product enum, the per-row product enum
    and the sanitiser's allowlist. It was absent from all three, so a J Bolt
    was not something the model was permitted to answer.
  · the instruction filing a drawn "L / J / U bend" under L_BOLT replaced with
    two rules: a hook returning UP alongside the shank is a J_BOLT, a 90-degree
    perpendicular leg is an L_BOLT
  · RECOGNISING A J BOLT ON A DRAWING — what a hook looks like on a sheet, and
    that any one of the return leg / inside width / hook outranks the title
  · READING A J BOLT'S DIMENSIONS — H is the overall height however the drawing
    labels it; a radius is not an inside diameter and is not converted into one
  · A TABLE MAY BE PRINTED SIDEWAYS — orientation from the table's own headings,
    rows as items after turning it, no value changed by rotation
  · WHAT A SPECIFICATION CELL ACTUALLY COVERS — which ROWS (the merge) and which
    PART (the rod, not its nuts and washers)
  · A ROW'S OWN SPECIFICATION BEATS A GENERAL REMARK
  · A STRENGTH CLASS IS NOT A MATERIAL, AND NEITHER IS A FINISH — 8.8 is not
    4140, HDG is a finish, A2/A4 are stainless classes, "A4" beside a paper word
    is the sheet
  · per-product field list and row-product wording extended with L_BOLT/J_BOLT

index.php
  · a stated return height (S) alone classifies a J Bolt. It used to need an ID
    as well, which a hook dimensioned by radius never states.
  · on a J Bolt with no stated height, the drawing's overall L IS the height and
    is reported as H — gated on the geometry, recorded as hFromL, shown on the
    row. L stays L on every other product.
  · a merged specification cell now carries down the rows it covers: material,
    finish and size type join M and TL in the row-to-row inheritance, replaced
    by any row that states its own, and only where the sheet is silent
  · wqaDetectCommon takes opts.noStrengthAlloy, set only by the document path:
    a strength class is kept as evidence and never answered with an alloy. The
    pasted-message G8.8 -> 4140 QT mapping is untouched.
  · A2 / A2-70 / A4 / A4-80 added as the ISO 3506 stainless classes, with a
    paper-size guard (notNear, new in wqaMatchMaterial)
  · row evidence fields: grade, radius, hFromL, finishSeen, specRaw — the raw
    wording kept beside the value it became
  · four badges and their English and Chinese strings

tests/
  · suites/19-drawing-interpretation.test.js — new, 146 assertions: both
    reported cases, the explicit-ID variant, entity scoping, remark precedence,
    the A2/A4 vocabulary, the paper trap and the negative tests
  · php/ai_extract.test.php — +38 assertions on the prompt, schema and sanitiser
  · lib/harness.js — exposes the new evidence fields
  · extraction-shots.js, extraction-evidence.js — new evidence generators

PASS 7 PART 2 — Quick Add insertion blocker and the stainless rule
-----------------------------------------------------------------
Baseline f074b5b111fc93698968ae704fc1b3443cab6ac2

index.php
  · DC_TYPED_RATE_PRODUCTS — the five products whose rate AND surcharge are
    typed, shared with syncCostRateWarning instead of written out three times
  · dcRateRequired(type): a rate is required because the price is COMPUTED from
    it, so a Manual Price — which is how a reused historical price arrives —
    needs none. Applied at all eight blank-rate guards. This is the reported
    blocker: a J Bolt priced RM 7.40 off a previous quotation could not be
    added, because a J Bolt never gets an automatic rate.
  · wqaRowMissing asks for the same two boxes the add path insists on, on the
    row, where a person can answer them. Nothing is auto-filled.
  · showToast collects the calculators' own refusal messages while an add is
    being attempted (dcToastCapture); they used to be overwritten by the
    summary before anyone could read one
  · wqaAddAll attempts every row, removes the ones that went in, keeps the ones
    that did not with the reason beside them, and no longer closes the review
    over the top of them. One blocked row no longer returns silently.
  · wqaUpdateAddButton enables Add while at least one row can go
  · applyDefaultPrice records the fields the rule actually WROTE, carried across
    the calls that write nothing. It used to adopt the thread-length surcharge
    and delete it on the next identity change, so a two-row Quick Add quoted
    RM 1.33 for a rod the calculator quotes at RM 1.93.
  · the stainless rule, applied where it was only a DOM side effect before:
    dcFormMaterial reads a Welding Anchor Set's material from its anchor;
    updateFinishAvailability uses dcMaterialHasFinish; onWASAnchorSpecChange
    runs it; pushItem, addWAS, the saved-quotation handoff and phFormSpec apply
    dcFinishFor; the draft restore applies the rule AFTER re-checking the saved
    radios rather than before
  · two i18n strings in both languages

pricing_history.php
  · DC_NO_FINISH_MATERIALS and dc_finish_for, mirroring the browser constant
  · both sides of the finish comparison in dc_history_record normalised, so a
    stainless record stored wearing HDG is no longer invisible to its own
    specification

tests/
  · suites/20-quickadd-add.test.js — new, 160 assertions: the reported failure,
    every Quick Add product, Apply-to-All, per-row edit, multi-row and partial
    add, the reason shown by name, and Quick Add priced against the Calculator
  · suites/21-stainless-finish.test.js — new, 115 assertions: the rule from the
    review row to the print sheet, the WhatsApp text and history identity
  · php/pricing_history.test.php — +18 assertions on the server-side rule
  · suites/17-quickadd-layout.test.js — a J Bolt now asks for the surcharge too
  · add-shots.js — new, the twelve before/after Add frames

PASS 7 PART 3 — material identity
---------------------------------
Baseline 581d5024e0e68426de27016d742ed28139919934

index.php
  · the stainless rules moved AHEAD of the strength families in WQA_MATERIALS.
    The loop stops at the first match, so their position IS the precedence
    rule: SUS304 written beside GRADE 8.8 is stainless, and A2-70's 70 is a
    property class rather than a grade.
  · both grade patterns extended — Class/CL, H.T. with its stops, High Tensile
    Grade, DIN 975 in every spelling, AISI 4140/4340, Q&T — and the bare HT and
    HIGH TENSILE alternatives REMOVED. Both 8.8 and 10.9 are high tensile, so
    the wording alone does not say which of two different steels the company
    buys; it stays null and Review asks.
  · AISI 304 / AISI 316 added to the stainless spellings; hot-dip with a hyphen
    and self colour added to the finishes
  · opts.noStrengthAlloy removed from wqaDetectCommon and from both call sites
    in wqaNormalizeExtraction. A grade means the same thing on a drawing as in
    a typed message: 8.8 is 4140 QT, 10.9 is 4340 QT, one mapping for every
    source. The class is kept beside the answer as evidence, not in place of it.
  · the material's provenance (matFrom, matDefaulted) now travels with it down a
    merged specification block, so row four of a cell says its 4140 QT came from
    the block's GRADE 8.8

ai_extract.php
  · the instruction block rewritten: copy the specification, do NOT translate it
    into an alloy — that mapping is the company's and runs in our own code.
    HT8.8, AISI 304/316 and A4-70 named in the vocabulary it lists.

tests/
  · suites/22-material-identity.test.js — new, 236 assertions: all 76 spellings
    one at a time, the four identities held distinct, the ambiguous wordings and
    paper traps left unresolved, precedence, grade-vs-finish pairs, entity
    scoping, both live production formats, and a door-to-door comparison proving
    a document and a message reach the same answer
  · suites/19-drawing-interpretation.test.js — Case B rows 3-6 are now 4140 QT
    with the class as evidence, and the assembly-cell test expects the stud's
    own 8.8 to name its material
  · php/ai_extract.test.php — prompt assertions updated, +5
  · lib/harness.js — exposes matFrom / matDefaulted
  · extraction-shots.js, extraction-evidence.js — the two live formats added

PASS 7 PART 4 — previous price: reuse and retrieval
---------------------------------------------------
Baseline ddc2c87666244aad786ee4a3add14f8e1897847c

index.php
  · wqaHistUse restores the record's PRICING RECIPE — cost rate, additional
    cost, markup and the calculated mode it was rounded in — and the row then
    prices itself from its own weight. It used to write the historical FINAL
    figure into Manual Price and leave the row's unrelated rate behind it, so
    the same basis applied to a different rod quoted the old rod's price.
  · WQA_CALC_MODES: auto and no_round are restored as themselves; nothing is
    forced to Auto Round and an unknown mode is not guessed
  · r.histApplied records which components a reuse installed, so a second
    record replaces exactly those — and nothing a person typed
  · a manual-priced record is still reused as a manual price; one saved with
    only a final figure says so; neither manufactures a rate
  · wqaApplyPriceToAll drops the history provenance only where it actually
    supersedes the recipe. "Not manual" was the old test, and a reused recipe
    leaves the row on Auto Round.
  · the row badge says which was reused — "pricing" or "price"

pricing_history.php
  · dc_legacy_item reads the material as everything before the size type. The
    old single-token pattern could not read "4140 QT" or "4340 QT", so every
    legacy record for two of the four canonical materials was invisible.
  · dc_material_code maps a printed label back to the canonical code
  · dc_history_sql_where / dc_history_blob_matches — one definition of which
    quotations to decode, narrowing on the SIZE only, in either representation.
    The old prefilter also demanded the material, and sent a quotation down the
    legacy branch only when the whole blob contained no cleanSize anywhere — so
    a legacy line inside an otherwise modern quotation was unreachable.
  · dc_history_material_needle deleted

api.php
  · get_pricing_history builds its WHERE from dc_history_sql_where. There is no
    recency window and there never should be.

tests/
  · suites/23-history-recipe.test.js — new, 68 assertions, clicking the real
    Use this price button: the two production records, a different geometry, a
    manual record, a legacy record, an unseparable one, the save/reload round
    trip, and the Calculator asserted to offer no reuse button
  · php/pricing_history.test.php — +30: legacy multi-word materials, thirteen
    negative material pairs, the prefilter predicate, and three hundred newer
    unrelated quotations that are not asked for while the older match is
  · suites/05 and 16 — the two reuse assertions updated to the new contract,
    asserting more than they did before
  · history-shots.js — new, the seven before/after frames
