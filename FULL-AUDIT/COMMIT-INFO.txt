QUOTATION.DNC — FULL-SYSTEM AUDIT, REPAIRS AND WORKFLOW POLISH
==============================================================

<<<GENERATED:HEADER>>>
BRANCH          claude/new-session-ofny46
BASELINE SHA    f96714e33795e80b581b1d03deb9d04db1d94b8d
                "Audit package: the polish, and what it must not have moved"
APPLICATION COMMIT
                7f5bc977197a658d6d4db995ee2c9bb5e106e21b
                "COMMIT-INFO stops naming the commit it is inside"
                The commit the accepted implementation and its test
                matrix were measured at. Every figure in this package
                was produced by this tree.
                Application SOURCE last changed one commit earlier, at
                67d4980 — "COMMIT-INFO stops naming the commit it is inside".
                Everything after it is evidence tooling and reports,
                and no application behaviour has moved since.
PACKAGE / HEAD COMMIT
                Not named here, and deliberately. The commits after the
                application one carry this package, and a document
                cannot name the commit it is inside without changing
                it. The exact HEAD an archive was built from is recorded
                in that archive's own MANIFEST/MANIFEST.txt, generated
                at build time and never committed — the one place that
                can honestly state it.
DEPLOYED        NO — nothing was deployed, and nothing was run against
                production data. Every test drives the shipped code
                against a controlled API in a local browser.
<<<END:HEADER>>>

WORKING TREE    clean at the final commit

There is exactly ONE application SHA and it is the one above. Every report in
this package quotes it and no other, and every number in this package was
measured against it.

The commits after it write this package. A report cannot name the commit it is
inside without changing that commit, so the exact HEAD the archive was built
from is recorded in the archive's own MANIFEST/MANIFEST.txt, generated at build
time and never committed.

PRICING — UNCHANGED THIS ROUND
------------------------------
The pricing formula, Auto Round, the weight formula, Cost Rate, Additional
Cost, Markup and Previous Price are byte-for-byte unchanged by the closing
repair. The manually verified case still answers what it answered:

    M24 x 1000 · 3.5513 kg/pc · Cost Rate 2.80 · Additional Cost 0.60
    Markup 4%   ->  3.5513 x 2.80 = 9.9436  + 0.60 = 10.5436
                    + 4% = 10.9653   Auto Round  =>  RM 10.97

Evidence frame F states that calculation on one screen and FAILS the run if the
answer ever stops being RM 10.97.

PRICING — UNCHANGED BY THE WORKFLOW POLISH ROUND TOO
----------------------------------------------------
The polish round separated three editing mechanisms, added row selection and
fixed four defects. It did not touch the pricing formula, the weight formula,
Auto Round, Cost Rate, Additional Cost, Markup, the diameter tables or the
parser. The one change that comes NEAR pricing is F31, and it removes a false
claim rather than a number: a row bulk-changed off the identity its historical
record described stops crediting that record. The rates the record contributed
stay on the row, because by then they are the row's own pricing entry.

AUDIT COMMITS (oldest first — this list is the count)
-----------------------------------------------------
<<<GENERATED:COMMITS>>>
  1cea497  Fifteen thousand is not fifteen, and one is not none
  5591772  One rod, one diameter, and a product word that is a word
  8e1bfff  The screens the language switch never reached, and a reference that was markup
  1f41833  The evidence, and the widths it was checked at
  a10e92c  The re-audit, and the archive that is the commit
  40e56d6  An ambiguous count is not a count, and a switch is not a re-render
  3696f59  The package, told from one SHA
  a764134  The final SHA is the one that includes this line
  772716f  The SHA a report can honestly name
  82ddc1d  The manifest names the commit, so it cannot be in it
  dd15663  The screen, read in 中文, and the hook nothing ever applied
  d2069c2  The closing round, told from one SHA and one set of counts
  4907667  The logs the reports name, actually inside the package
  4210c1b  Quick Add is called Quick Add, and its scope is the one the parser has
  2aaceb1  The package, told from the SHA that includes the copy change
  50787d1  ERP polish: hierarchy, states and one sentence under a grey button
  7945bf9  The package, told from the polish SHA
  d4a4cd0  What a row is priced on, without opening it
  04e4159  The package, told from the row-summary SHA
  c008c88  A size is a thread; a diameter is a bar
  6b94cd3  One edit state, three doors into it
  55b21c4  Expanded stops repeating what the grid already edits
  822fe13  The package, told from the diameter-authority SHA
  cc7f994  Escape gives back the bar and where it came from
  7cc1ccf  Three mechanisms, three jobs, no overlap
  0ac5fc5  The new surfaces, scanned in 中文 rather than assumed
  409f664  The refusal beside every Apply it refuses
  2aa5aba  The ticks measured at all seven widths, not counted
  55a99c2  The package, told from the workflow-polish SHA
  61d4fef  Closing repairs, and two things the probes reproduced
  414ea18  An item number written as a word is not a dimension
  d027535  One archive, laid out for a reviewer, and nine frames that prove the repairs
  6d751d1  The package, told from the closing-repair SHA
  c5ec93e  The reports, agreeing with the commit that produced them
  67d4980  COMMIT-INFO stops naming the commit it is inside
  7f5bc97  COMMIT-INFO stops naming the commit it is inside
<<<END:COMMITS>>>

  1cea497  The three Priority Zero items, plus the two comma defects behind
           them. Thousands separators in wqaNorm; the spec-header reader made
           to use the helper wqaExtractFields already uses; the approved
           absent-Qty rule in the one normaliser every source passes through,
           with absent told apart from unreadable; the context-sensitive
           Thread Reference hint. And 127 user-visible strings moved out of
           showToast and title attributes into the dictionary.

  5591772  Inch spellings canonicalised so 1", 1 IN and 1 INCH are one size
           with one fullsize and one undersize bar; the product alias matcher
           made word-bounded so SPECIAL BOLT is not an L Bolt; L BOLT 45DEG no
           longer priced with the 90-degree bend deduction.

  8e1bfff  The Pricing Guide, the Plate and Welding Anchor Set forms, the
           empty states and the short labels. The deliberate exclusions
           written into the checker. And the quotation reference on the
           Companies page, which reached innerHTML unescaped.

  1f41833  The responsive suite at the seven widths the brief names, the
           32-frame evidence set, and the audit package.

  a10e92c  The full matrix re-run from a clean tree, and the archive built
           with git archive from HEAD.

  40e56d6  MORNING REPAIR. The ambiguous quantity, which resolved itself to
           one piece and spawned a phantom row. The language switch that wiped
           the live item count. The item cards that were never re-rendered, so
           they read "Edit / 编辑". The translation checker that reported zero
           while all of that was on screen — and everything it then found.

  dd15663  FINAL CLOSING REPAIR. The rendered-DOM scan, and what it found on a
           screen two rounds had signed off: 38 English runs in 中文 mode,
           including all four the review named by hand. Behind almost all of
           them, one defect — a data-i18n on generated markup is a hook that
           nothing will ever apply, because dcApplyLang is an attribute scan
           that runs once and does not come back. 63 elements were built that
           way. And one that is not about language at all: ten of the eleven
           product forms lost their Price Mode control in 中文, because the
           injector looked its anchor up by the English word "Pricing".

  4210c1b  UI COPY. The homepage entry called "WhatsApp Quick Add" when a
           photograph, a screenshot, a drawing and a PDF go through the same
           door — renamed Quick Add / 快速添加, with a subtitle that says what
           actually goes in. And the product scope, which named three products
           while WQA_PRODUCTS reads five: off the button, stated once inside
           the modal beside the box it describes, and complete. Suite 33 §2b
           asserts that list against WQA_PRODUCTS itself, not a copy.

  50787d1  UI/UX POLISH. The modal retitled Quick Add / 快速添加 to match its
           button; the paste hint GENERATED from WQA_PRODUCTS so no second
           list can fall out of date. Section labels give the expanded form a
           readable hierarchy; the warnings become one filled badge plus
           outline field tags; a blocked footer says what to do about it; the
           scope toggle joins Bulk Edit under "Apply to:"; buttons gain press
           and focus states; filenames truncate with the full name in the
           tooltip; History leads with the RM price. No calculated value,
           parser result, quantity, weight or price is touched.

  d4a4cd0  COMPACT ROW PRICING SUMMARY. A read-only two-column line under
           every compact row: where the size type came from and which record
           a reused price came from on the left; Cost Rate, Additional Cost
           and Markup on the right. Every figure is READ off r.calc — the
           same object the expanded form edits and the calculator fills — so
           there is no second formula to drift. Patched alongside the price,
           so typing a rate or applying a Previous Price moves it at once.
           Two whole-card shape budgets in suites 17 and 18 were replaced by
           per-part ceilings (data line 46, identity strip 26, summary 40),
           because a requested second line changed the shape those numbers
           described.

  c008c88  DIAMETER AUTHORITY. A size is a thread; a diameter is a bar, and
           they are different numbers. A DIA column beside SIZE, showing the
           diameter the weight was actually made of — read back OUT of the
           calculator after the calculation, so it cannot be a claim about
           what was used. A typed diameter outranks the table for that row and
           drives the weight; size identity does not move, and Previous Price
           still matches on M12. Changing size, size type or material drops
           the override. No bar means a dash and Needs Diameter, never a
           guess. The hidden 10.7-vs-10.6 split the brief asked about was
           RATES.dia, already deleted in 8e1bfff — verified, not assumed.

  6b94cd3  FAST EDIT. One edit state with three doors: the Edit button, a
           value, or a "Needs X" tag. Every editable cell of every row becomes
           an input at once. Missing may leave; invalid may not. Cancel
           restores a snapshot. Eight protections while it is open —
           Expanded, Add, History and Previous Price apply, Bulk Edit, Common
           Fields, re-parse, Delete — gathered in one function so the list
           stays readable.

  55b21c4  EXPANDED without the duplicate Dimensions block, plus suites 35 and
           36. Thread Reference keeps a Reference section; Calculation leads
           with the Actual Diameter. Every expected weight in suite 36 is
           computed from the number on the screen, with the constant checked
           against pi/4 x 7.85e-6 rather than borrowed.

  cc7f994  F30. Escape restored a diameter's value by handing the snapshot to
           wqaEditDia, whose job is to record that a person typed it -- so
           restoring the table's own 10.6 announced it as an override, and the
           next size change would have dropped an override nobody made. Both
           fields restored together; the mark made visible on the box, because
           the one mode where provenance can change was the one place you
           could not see it. The failing assertion was written first.

  7cc1ccf  The three mechanisms separated. The row action became Details and
           the toolbar action kept Edit. Inside Bulk Edit the two panels had
           each other's names: "Correct Items" held Product, Material, Finish
           and Size Type -- shared identity, the definition of a bulk field --
           while "Common Item Fields" bulk-edited Size and TL, which is
           geometry. Names put on the right panels; the geometry panel
           confined to the documented case (a shorthand document that states
           neither) and rendered only when rows are actually missing one.
           Sections open one at a time. Selection became a persistent set: a
           tick on every row, a select-all in the header, a count on the
           heading, ticks that survive the scope switch, and a refusal instead
           of a silent widening when "Selected" has nothing selected. F31 and
           F32 found while wiring it.

  0ac5fc5  F33 and one mixed-language leak, both found by scanning the
           RENDERED DOM in 中文 rather than the source. Suite 33 grew a block
           for every surface this round added.

  409f664  The refusal put beside every Apply it refuses -- an evidence frame
           caught two panels disabling a button and explaining nothing. That
           frame now asserts the message, not only the disabled state.
           Packaging became a script that reads every entry with
           git show HEAD:<path> and writes full 64-character digests.

FILES CHANGED (baseline -> final)
---------------------------------
   companies.php                              |  240 ++-
   index.php                                  | 2368 ++++++++++++++++++++++------
   tests/audit-shots.js                       |  789 +++++++++
   tests/lib/dom-i18n.js                      |  243 +++
   tests/lib/harness.js                       |  108 +-
   tests/suites/01-size-normalisation.test.js |    8 +-
   tests/suites/03-weight.test.js             |    7 +-
   tests/suites/08-panels.test.js             |    6 +-
   tests/suites/17-quickadd-layout.test.js    |   29 +-
   tests/suites/18-quickadd-stress.test.js    |   25 +-
   tests/suites/26-thread-reference.test.js   |   69 +
   tests/suites/29-quantity.test.js           |  378 +++++
   tests/suites/30-language.test.js           |  362 +++++
   tests/suites/31-size-system.test.js        |  246 +++
   tests/suites/32-responsive.test.js         |  124 ++
   tests/suites/33-rendered-i18n.test.js      |  500 ++++++
   tests/suites/34-row-meta.test.js           |  248 +++
   tests/suites/35-edit-mode.test.js          |  369 +++++
   tests/suites/36-diameter.test.js           |  269 ++++
   tests/tools/check-translations.js          |  631 ++++++++
   20 files changed, 6406 insertions(+), 613 deletions(-)

TEST MATRIX AT FINAL SHA
------------------------
<<<GENERATED:MATRIX>>>
  Browser suites            37 suites, 3,613 assertions, 0 failed
  Pricing-history PHP                    161 assertions, 0 failed
  AI extraction PHP                      107 assertions, 0 failed
  Pricing workbook                        62 assertions, 0 failed
  Translation coverage                    15 assertions, 0 failed
  ----------------------------------------------------------------
  TOTAL ASSERTIONS                      3,958
  TOTAL FAILED                              0
  SKIPPED / ENVIRONMENT-LIMITED             0

  Baseline:   2,810 assertions
  Final:      3,958 assertions
  Delta:     +1,148 assertions
<<<END:MATRIX>>>

  php -l over every PHP file  clean
  Browser console errors      none, asserted per page at every viewport
  Rendered 中文 DOM           12 states, 0 English runs outside the allowlist

FINDINGS
--------
<<<GENERATED:FINDINGS>>>
  P0  0
  P1  13  all repaired
  P2  24  all repaired
  P3  2   all repaired
  ---------------------
  39 total, 39 repaired.  6 recorded-not-repaired with reasons.
  2 questions still need a business answer; 4 earlier ones are now decided
  and are recorded as decided rather than counted as open.
<<<END:FINDINGS>>>

EVIDENCE
--------
<<<GENERATED:EVIDENCE>>>
  screenshots/          96 frames + INDEX.txt
  before-fix/           6 frames from the baseline commit, same inputs
  after-fix/            56 frames, the matching after
  regression-evidence/  16 files — every log the reports claim,
                        checked against the directory rather than written
                        from memory
<<<END:EVIDENCE>>>

LINE ENDINGS
------------
  index.php and companies.php are uniformly CRLF at the final commit, as they
  were at the baseline. Checked before every commit; the one earlier lapse
  (LF conversion by a bulk-edit script) was caught and restored in 8e1bfff,
  and every edit script in the closing round normalises its own patterns to
  CRLF before matching so it cannot repeat.
